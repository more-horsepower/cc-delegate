[tool.pytest.ini_options]
testpaths = ["tests"]
markers = [
    "live: tests that require the real opencode binary (Tier 2+3 contract probes)",
]
filterwarnings = [
    "ignore::DeprecationWarning",
]
